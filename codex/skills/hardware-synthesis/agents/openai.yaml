# Codex-specific metadata (ignored by Claude Code and other agents).
interface:
  display_name: "Hardware Synthesis"
  short_description: "Synthesize RTL and report utilization"
policy:
  # implicit = Codex may auto-trigger from a matching prompt;
  # explicit-only (hardware-synthesis) for slow/expensive stages.
  allow_implicit_invocation: true
