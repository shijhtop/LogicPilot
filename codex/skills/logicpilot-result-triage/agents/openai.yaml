# Codex-specific metadata (ignored by Claude Code and other agents).
# Auto-generated by sync_package.py; edit and it will be preserved on re-sync.
interface:
  display_name: "Logicpilot Result Triage"
  short_description: "Use when interpreting LogicPilot JSON output from lp-run, lp-front, lp-doctor, o"
policy:
  # implicit = Codex may auto-trigger from a matching prompt.
  allow_implicit_invocation: true
