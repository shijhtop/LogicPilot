# Codex-specific metadata (ignored by Claude Code and other agents).
interface:
  display_name: "Hardware Simulation"
  short_description: "Run and debug RTL simulation"
policy:
  # implicit = Codex may auto-trigger from a matching prompt;
  # explicit-only (hardware-simulation) for slow/expensive stages.
  allow_implicit_invocation: true
