# Codex-specific metadata (ignored by Claude Code and other agents).
interface:
  display_name: "SystemVerilog Verification Platform"
  short_description: "Build SystemVerilog verification testbenches"
policy:
  # implicit = Codex may auto-trigger from a matching prompt.
  allow_implicit_invocation: true
